/**
 * Empty implementation of the session check function
 * This is kept as a stub to prevent breaking existing imports
 */
export const initSessionCheck = () => {
    // No-op function - session checking functionality has been disabled
    return () => {};
};
